<% count=0 %>
<table class="table table-striped table-hover ">
  <thead>
  <tr>
    <th>№</th>
    <th>ID</th>
    <th>original_text</th>
    <th>translated_text</th>
    <th>review_date</th>
  </tr>
  </thead>
  <tbody>
  <% @cards.each.with_index(1) do |card, index| %>
  <tr>
    <td><%= index %></td>
    <td><%= card.id %></td>
    <td><%= card.original_text || "null" %></td>
    <td><%= card.translated_text || "null" %></td>
    <td><%= card.review_date.present? ? (l card.review_date, format: :default) : "null" %></td>
  </tr>
  <% end %>
  </tbody>
</table>


